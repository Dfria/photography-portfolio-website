{
  "name": "portfolio-website",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "A static photo & video portfolio site with album folders, deployed on GitHub Pages.",
  "scripts": {
    "manifest": "node scripts/generate-manifest.js",
    "predev": "npm run manifest",
    "dev": "vite",
    "prebuild": "npm run manifest",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
